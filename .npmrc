# Disable strict peer dependencies to allow React 19
strict-peer-dependencies=false

# Auto install peers
auto-install-peers=true

# Use shamefully-hoist to avoid hoisting issues
shamefully-hoist=true
